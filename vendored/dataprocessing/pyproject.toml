[project]
name = "dataprocessing"
description = "Code for processing data"
readme = "README.md"
version = "0.1.0"
dependencies = [
    "gcsfs>=2026.3.0",
    # Google Earth Engine Python API — used by scripts/gee/fetch_station_mtpi.py
    # to sample the ALOS Global mTPI product at station locations.
    "earthengine-api>=1.0",
]
