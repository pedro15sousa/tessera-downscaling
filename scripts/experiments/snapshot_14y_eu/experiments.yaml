# snapshot_14y_eu — shortlisted experiments (reconstructed)
#
# Only the experiments that appear in the notebook shortlist screenshots
# (live entries + their non-pending siblings) are retained.
# Original 28-config matrix + ablations have been pruned down to the
# subset actually needed for the paper.

# =========================================================================
# Single-task baselines — t2m
# =========================================================================

- name: t2m_snap_era5_interp_baseline
  label: "t2m snap ERA5 interpolation (no model, K→°C)"
  colour: "#cccccc"
  target_variables: [t2m]
  extra_args: ""
  baseline_kind: era5_interp

# Lapse-rate-corrected ERA5 interpolation. t2m only — wind speed has no
# lapse-rate analogue, and the script refuses non-t2m targets rather than
# emitting a row numerically identical to plain era5_interp.
#   fixed  : the textbook 6.5 K/km environmental lapse rate.
#   fitted : Gamma estimated by least squares on the TRAIN split (train
#            stations, train years) — the strongest honest version of the
#            elevation-only correction. Resolved Gamma and its fit
#            diagnostics land in test_summary.json under "lapse_rate".
# Reporting both shows the row is not sensitive to the choice of Gamma.
- name: t2m_snap_era5_interp_lapse_baseline
  label: "t2m snap ERA5 interp + lapse rate (fixed 6.5 K/km)"
  colour: "#c7b299"
  target_variables: [t2m]
  extra_args: "--lapse-rate-mode fixed"
  baseline_kind: era5_interp_lapse

- name: t2m_snap_era5_interp_lapse_fitted_baseline
  label: "t2m snap ERA5 interp + lapse rate (fitted on train)"
  colour: "#a68b6d"
  target_variables: [t2m]
  extra_args: "--lapse-rate-mode fitted --lapse-fit-stride 4"
  baseline_kind: era5_interp_lapse

- name: t2m_snap_persistence_baseline
  label: "t2m snap persistence (most recent valid obs ≤24h)"
  colour: "#dddddd"
  target_variables: [t2m]
  extra_args: ""
  baseline_kind: persistence

- name: t2m_snap_bilinear_baseline_wd
  label: "t2m snap bilinear baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4"

- name: t2m_snap_setconv_baseline_wd
  label: "t2m snap SetConv baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [t2m]
  extra_args: "--interpolation setconv --weight-decay 1e-4"


# =========================================================================
# Single-task baselines — wind (gaussian)
# =========================================================================

- name: wind_snap_era5_interp_baseline
  label: "Wind snap ERA5 interpolation (no model, sqrt(u²+v²))"
  colour: "#cccccc"
  target_variables: [wind]
  extra_args: ""
  baseline_kind: era5_interp

- name: wind_snap_persistence_baseline
  label: "Wind snap persistence (most recent valid obs ≤24h)"
  colour: "#dddddd"
  target_variables: [wind]
  extra_args: ""
  baseline_kind: persistence

- name: wind_snap_bilinear_baseline_wd
  label: "Wind snap bilinear baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4"

- name: wind_snap_setconv_baseline_wd
  label: "Wind snap SetConv baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [wind]
  extra_args: "--interpolation setconv --weight-decay 1e-4"


# =========================================================================
# Single-task baselines — wind (weibull)
# =========================================================================

- name: wind_weibull_snap_era5_interp_baseline
  label: "Wind (Weibull) snap ERA5 interpolation (no model)"
  colour: "#cccccc"
  target_variables: [wind]
  extra_args: ""
  baseline_kind: era5_interp

- name: wind_weibull_snap_persistence_baseline
  label: "Wind (Weibull) snap persistence (most recent valid obs ≤24h)"
  colour: "#dddddd"
  target_variables: [wind]
  extra_args: ""
  baseline_kind: persistence

# - name: wind_weibull_snap_bilinear_baseline_wd
#   label: "Wind (Weibull) snap bilinear baseline (with elev, with static, wd=1e-4)"
#   colour: "#999999"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=weibull"


# =========================================================================
# Single-task baselines — precip (Bernoulli-Gamma)
# =========================================================================

- name: precip_snap_era5_interp_baseline
  label: "Precip (B-G) snap ERA5 interpolation (no model)"
  colour: "#cccccc"
  target_variables: [precip]
  extra_args: ""
  baseline_kind: era5_interp

- name: precip_snap_persistence_baseline
  label: "Precip (B-G) snap persistence (most recent valid obs ≤24h)"
  colour: "#dddddd"
  target_variables: [precip]
  extra_args: ""
  baseline_kind: persistence

- name: precip_snap_bilinear_baseline_wd
  label: "Precip (B-G) snap bilinear baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood precip=bernoulli_gamma"


# =========================================================================
# Single-task — t2m top-5 vanilla TESSERA (VAE) + kernel-swap siblings
# =========================================================================

- name: t2m_snap_vae_lat64_proj16_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat64→proj16 concat (with elev, no static, wd=1e-4)"
  colour: "#2ca02c"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat16_proj8_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat16→proj8 concat (with elev, no static, wd=1e-4)"
  colour: "#1f77b4"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat64_proj16_setconv_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat64→proj16 SetConv concat (wd=1e-4)"
  colour: "#444444"
  target_variables: [t2m]
  extra_args: "--interpolation setconv --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat16_proj8_setconv_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat16→proj8 SetConv concat (wd=1e-4)"
  colour: "#444444"
  target_variables: [t2m]
  extra_args: "--interpolation setconv --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat64_proj16_bilinear_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat64→proj16 Bilinear concat (wd=1e-4)"
  colour: "#7f7f7f"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat16_proj8_bilinear_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat16→proj8 Bilinear concat (wd=1e-4)"
  colour: "#7f7f7f"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — t2m shuffled-latents controls
# =========================================================================

- name: t2m_snap_vae_lat64_proj16_shuffled_setconv_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat64→proj16 shuffled SetConv concat (wd=1e-4)"
  colour: "#98df8a"
  target_variables: [t2m]
  extra_args: "--interpolation setconv --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat64_proj16_shuffled_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat64→proj16 shuffled concat (wd=1e-4)"
  colour: "#98df8a"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_vae_lat16_proj8_shuffled_bilinear_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat16→proj8 shuffled Bilinear concat (wd=1e-4)"
  colour: "#98df8a"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — t2m stats-encoder counterparts
# =========================================================================

- name: t2m_snap_stats_dim64_proj16_setconv_concat_with_elev_no_static_wd
  label: "t2m snap summary-stats dim64→proj16 SetConv concat (wd=1e-4)"
  colour: "#17becf"
  target_variables: [t2m]
  extra_args: "--interpolation setconv --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_stats_dim64_proj16_concat_with_elev_no_static_wd
  label: "t2m snap summary-stats dim64→proj16 concat (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: t2m_snap_stats_dim16_proj8_concat_with_elev_no_static_wd
  label: "t2m snap summary-stats dim16→proj8 concat (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — wind (gaussian) top-5 vanilla TESSERA (VAE)
# =========================================================================

- name: wind_snap_vae_lat16_proj8_mlp_film_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8-MLP FiLM (with elev, no static, wd=1e-4)"
  colour: "#ff7f0e"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_proj8_mlp_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8-MLP concat (with elev, no static, wd=1e-4)"
  colour: "#1f77b4"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16 concat (with elev, no static, wd=1e-4)"
  colour: "#1f77b4"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_proj8_film_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8 FiLM (with elev, no static, wd=1e-4)"
  colour: "#ff7f0e"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_proj8_bilinear_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8 Bilinear concat (wd=1e-4)"
  colour: "#7f7f7f"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — wind (gaussian) kernel-swap siblings
# =========================================================================

- name: wind_snap_vae_lat16_proj8_setconv_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8 SetConv concat (wd=1e-4)"
  colour: "#444444"
  target_variables: [wind]
  extra_args: "--interpolation setconv --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_setconv_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16 SetConv concat (wd=1e-4)"
  colour: "#444444"
  target_variables: [wind]
  extra_args: "--interpolation setconv --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — wind (gaussian) shuffled-latents controls
# =========================================================================

- name: wind_snap_vae_lat16_proj8_shuffled_mlp_film_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8-MLP shuffled FiLM (wd=1e-4)"
  colour: "#ffbb78"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_proj8_shuffled_bilinear_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8 shuffled Bilinear concat (wd=1e-4)"
  colour: "#aec7e8"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_proj8_shuffled_mlp_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16→proj8-MLP shuffled concat (wd=1e-4)"
  colour: "#aec7e8"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4"

- name: wind_snap_vae_lat16_shuffled_concat_with_elev_no_static_wd
  label: "Wind snap VAE-lat16 shuffled concat (wd=1e-4)"
  colour: "#aec7e8"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — wind (gaussian) stats-encoder counterparts
# =========================================================================

- name: wind_snap_stats_dim16_proj8_mlp_film_with_elev_no_static_wd
  label: "Wind snap summary-stats dim16→proj8-MLP FiLM (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4"

- name: wind_snap_stats_dim16_proj8_concat_with_elev_no_static_wd
  label: "Wind snap summary-stats dim16→proj8 concat (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: wind_snap_stats_dim16_concat_with_elev_no_static_wd
  label: "Wind snap summary-stats dim16 concat (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Single-task — wind (weibull) top-4 vanilla TESSERA (VAE)
# =========================================================================

# - name: wind_weibull_snap_vae_lat16_concat_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat16 concat (with elev, no static, wd=1e-4)"
#   colour: "#1f77b4"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"

# - name: wind_weibull_snap_vae_lat16_proj8_mlp_film_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat16→proj8-MLP FiLM (with elev, no static, wd=1e-4)"
#   colour: "#ff7f0e"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"

# - name: wind_weibull_snap_vae_lat64_proj8_film_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat64→proj8 FiLM (with elev, no static, wd=1e-4)"
#   colour: "#17becf"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"

# - name: wind_weibull_snap_vae_lat64_proj16_concat_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat64→proj16 concat (with elev, no static, wd=1e-4)"
#   colour: "#2ca02c"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"


# =========================================================================
# Single-task — wind (weibull) shuffled-latents controls
# =========================================================================

# - name: wind_weibull_snap_vae_lat16_shuffled_concat_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat16 shuffled concat (wd=1e-4)"
#   colour: "#aec7e8"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"

# - name: wind_weibull_snap_vae_lat16_proj8_shuffled_mlp_film_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat16→proj8-MLP shuffled FiLM (wd=1e-4)"
#   colour: "#ffbb78"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"

# - name: wind_weibull_snap_vae_lat64_proj16_shuffled_concat_with_elev_no_static_wd
#   label: "Wind (Weibull) snap VAE-lat64→proj16 shuffled concat (wd=1e-4)"
#   colour: "#98df8a"
#   target_variables: [wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4 --likelihood wind=weibull"


# =========================================================================
# Single-task — precip (B-G) top-5 vanilla TESSERA (VAE)
# =========================================================================

- name: precip_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "Precip (B-G) snap VAE-lat16 concat (with elev, no static, wd=1e-4)"
  colour: "#1f77b4"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood precip=bernoulli_gamma"

- name: precip_snap_vae_lat64_proj16_concat_with_elev_no_static_wd
  label: "Precip (B-G) snap VAE-lat64→proj16 concat (with elev, no static, wd=1e-4)"
  colour: "#2ca02c"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4 --likelihood precip=bernoulli_gamma"

- name: precip_snap_vae_lat16_proj8_mlp_film_with_elev_no_static_wd
  label: "Precip (B-G) snap VAE-lat16→proj8-MLP FiLM (with elev, no static, wd=1e-4)"
  colour: "#ff7f0e"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood precip=bernoulli_gamma"

- name: precip_snap_vae_lat64_proj16_mlp_concat_with_elev_no_static_wd
  label: "Precip (B-G) snap VAE-lat64→proj16-MLP concat (with elev, no static, wd=1e-4)"
  colour: "#2ca02c"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood precip=bernoulli_gamma"

- name: precip_snap_vae_lat64_proj32_concat_with_elev_no_static_wd
  label: "Precip (B-G) snap VAE-lat64→proj32 concat (with elev, no static, wd=1e-4)"
  colour: "#2ca02c"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 32 --no-static-fields --weight-decay 1e-4 --likelihood precip=bernoulli_gamma"


# =========================================================================
# Multi-task baselines
# =========================================================================

- name: multitask_snap_bilinear_baseline_wd
  label: "Multi-task snap bilinear baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [t2m, wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4"

# - name: multitask_mixed_snap_bilinear_baseline_wd
#   label: "Multi-task mixed (G+W) snap bilinear baseline (with elev, with static, wd=1e-4)"
#   colour: "#999999"
#   target_variables: [t2m, wind]
#   extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull"

# - name: multitask_full_snap_bilinear_baseline_wd
#   label: "Multi-task full (G+W+BG) snap bilinear baseline (with elev, with static, wd=1e-4)"
#   colour: "#999999"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"


# =========================================================================
# Multi-task [t2m, wind] (gaussian) — top-5 vanilla TESSERA
# =========================================================================

- name: multitask_snap_vae_lat16_proj8_film_with_elev_no_static_wd
  label: "Multi-task snap VAE-lat16→proj8 FiLM (with elev, no static, wd=1e-4)"
  colour: "#ff7f0e"
  target_variables: [t2m, wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4"

- name: multitask_snap_vae_lat16_film_with_elev_no_static_wd
  label: "Multi-task snap VAE-lat16 FiLM (with elev, no static, wd=1e-4)"
  colour: "#ff7f0e"
  target_variables: [t2m, wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"

- name: multitask_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "Multi-task snap VAE-lat16 concat (with elev, no static, wd=1e-4)"
  colour: "#1f77b4"
  target_variables: [t2m, wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"

- name: multitask_snap_vae_lat64_proj16_film_with_elev_no_static_wd
  label: "Multi-task snap VAE-lat64→proj16 FiLM (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [t2m, wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"

- name: multitask_snap_vae_lat64_proj16_concat_with_elev_no_static_wd
  label: "Multi-task snap VAE-lat64→proj16 concat (with elev, no static, wd=1e-4)"
  colour: "#2ca02c"
  target_variables: [t2m, wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4"


# =========================================================================
# Multi-task mixed (G+W) — top-5 vanilla TESSERA
# =========================================================================

# - name: multitask_mixed_snap_vae_lat64_proj32_concat_with_elev_no_static_wd
#   label: "Multi-task mixed (G+W) snap VAE-lat64→proj32 concat (with elev, no static, wd=1e-4)"
#   colour: "#2ca02c"
#   target_variables: [t2m, wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 32 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull"

# - name: multitask_mixed_snap_vae_lat16_proj8_film_with_elev_no_static_wd
#   label: "Multi-task mixed (G+W) snap VAE-lat16→proj8 FiLM (with elev, no static, wd=1e-4)"
#   colour: "#ff7f0e"
#   target_variables: [t2m, wind]
#   extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull"

# - name: multitask_mixed_snap_vae_lat64_proj8_film_with_elev_no_static_wd
#   label: "Multi-task mixed (G+W) snap VAE-lat64→proj8 FiLM (with elev, no static, wd=1e-4)"
#   colour: "#17becf"
#   target_variables: [t2m, wind]
#   extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull"

# - name: multitask_mixed_snap_vae_lat64_proj16_concat_with_elev_no_static_wd
#   label: "Multi-task mixed (G+W) snap VAE-lat64→proj16 concat (with elev, no static, wd=1e-4)"
#   colour: "#2ca02c"
#   target_variables: [t2m, wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull"

# - name: multitask_mixed_snap_vae_lat16_proj8_concat_with_elev_no_static_wd
#   label: "Multi-task mixed (G+W) snap VAE-lat16→proj8 concat (with elev, no static, wd=1e-4)"
#   colour: "#1f77b4"
#   target_variables: [t2m, wind]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull"


# =========================================================================
# Multi-task full (G+W+BG) — union of precip and wind[weibull] top-5
# =========================================================================

# - name: multitask_full_snap_vae_lat64_proj32_concat_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat64→proj32 concat (with elev, no static, wd=1e-4)"
#   colour: "#2ca02c"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 32 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"

# - name: multitask_full_snap_vae_lat16_proj8_film_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat16→proj8 FiLM (with elev, no static, wd=1e-4)"
#   colour: "#ff7f0e"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"

# - name: multitask_full_snap_vae_lat16_proj8_mlp_concat_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat16→proj8-MLP concat (with elev, no static, wd=1e-4)"
#   colour: "#1f77b4"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"

# - name: multitask_full_snap_vae_lat16_proj8_concat_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat16→proj8 concat (with elev, no static, wd=1e-4)"
#   colour: "#1f77b4"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"

# - name: multitask_full_snap_vae_lat64_concat_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat64 concat (with elev, no static, wd=1e-4)"
#   colour: "#2ca02c"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"

# - name: multitask_full_snap_vae_lat16_concat_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat16 concat (with elev, no static, wd=1e-4)"
#   colour: "#1f77b4"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"

# - name: multitask_full_snap_vae_lat64_proj16_concat_with_elev_no_static_wd
#   label: "Multi-task full (G+W+BG) snap VAE-lat64→proj16 concat (with elev, no static, wd=1e-4)"
#   colour: "#2ca02c"
#   target_variables: [t2m, wind, precip]
#   extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_LAT64} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --weight-decay 1e-4 --likelihood t2m=gaussian,wind=weibull,precip=bernoulli_gamma"


# --- TruncNormal head — top-5 wind (post VAE-only filter) ---

- name: wind_truncnormal_snap_vae_lat16_proj8_mlp_film_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16→proj8-MLP FiLM (with elev, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: wind_truncnormal_snap_vae_lat16_proj8_concat_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16→proj8 concat (with elev, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: wind_truncnormal_snap_vae_lat16_proj8_mlp_concat_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16→proj8-MLP concat (with elev, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --vae-latents-proj-mlp --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16 concat (with elev, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: wind_truncnormal_snap_vae_lat16_proj8_film_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16→proj8 FiLM (with elev, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


  # =========================================================================
# CRPS-loss training experiment — backfill missing NLL archs first
# =========================================================================

- name: t2m_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#2ca02c"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"

- name: wind_truncnormal_snap_bilinear_baseline_wd
  label: "Wind (TruncNormal) snap bilinear baseline (with elev, with static, wd=1e-4)"
  colour: "#999999"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=truncated_normal"

# =========================================================================
# CRPS-loss variants
# =========================================================================

- name: t2m_snap_bilinear_baseline_wd_crps
  label: "t2m snap bilinear baseline + CRPS loss"
  colour: "#aaaaaa"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --loss-function crps"

- name: t2m_snap_vae_lat16_concat_with_elev_no_static_wd_crps
  label: "t2m snap VAE-lat16 concat direct + CRPS loss"
  colour: "#2ca02c"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --loss-function crps"

- name: t2m_snap_vae_lat16_proj8_concat_with_elev_no_static_wd_crps
  label: "t2m snap VAE-lat16→proj8 concat + CRPS loss"
  colour: "#1f77b4"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --loss-function crps"

- name: wind_truncnormal_snap_bilinear_baseline_wd_crps
  label: "Wind (TruncNormal) snap bilinear baseline + CRPS loss"
  colour: "#aaaaaa"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=truncated_normal --loss-function crps"

- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_no_static_wd_crps
  label: "Wind (TruncNormal) snap VAE-lat16 concat direct + CRPS loss"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal --loss-function crps"

- name: wind_truncnormal_snap_vae_lat16_proj8_concat_with_elev_no_static_wd_crps
  label: "Wind (TruncNormal) snap VAE-lat16→proj8 concat + CRPS loss"
  colour: "#9467bd"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 8 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal --loss-function crps"

# =========================================================================
# Generative head (implicit, CRPS-trained) — single-task per variable
#
# Implicit generator (MLP over [hidden_state, noise z]) trained by CRPS
# scoring-rule minimisation; captures arbitrary predictive shapes (skew,
# heavy tails, multimodality) the parametric families miss. The head has no
# tractable density, so --loss-function crps is mandatory (train.py refuses
# nll). Two arch flavors per variable: convcnp baseline (no TESSERA) and
# lat16 VAE-latent direct concat (no projection).
# =========================================================================

- name: t2m_generative_snap_bilinear_baseline_wd
  label: "t2m snap Generative-CRPS bilinear baseline (no tessera, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood t2m=generative --loss-function crps"

- name: t2m_generative_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "t2m snap Generative-CRPS VAE-lat16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood t2m=generative --loss-function crps"

- name: wind_generative_snap_bilinear_baseline_wd
  label: "wind snap Generative-CRPS bilinear baseline (no tessera, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=generative --loss-function crps"

- name: wind_generative_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "wind snap Generative-CRPS VAE-lat16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=generative --loss-function crps"

- name: precip_generative_snap_bilinear_baseline_wd
  label: "precip snap Generative-CRPS bilinear baseline (no tessera, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood precip=generative --loss-function crps"

- name: precip_generative_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "precip snap Generative-CRPS VAE-lat16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood precip=generative --loss-function crps"


# =========================================================================
# Generative head — NON-NEGATIVE variant (softplus output) for wind / precip
#
# Same implicit CRPS-trained generator as `generative`, but samples are passed
# through softplus so the predictive support is (0, inf) — matching the support
# of wind / precip and removing the sharpness handicap vs TruncNormal / B-G
# (which build the >=0 constraint in). t2m is real-valued and keeps the plain
# `generative` head. Two arch flavors per variable: convcnp baseline and
# lat16 VAE-latent direct concat.
# =========================================================================

- name: wind_generative_nonneg_snap_bilinear_baseline_wd
  label: "wind snap Generative-nonneg-CRPS bilinear baseline (no tessera, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=generative_nonneg --loss-function crps"

- name: wind_generative_nonneg_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "wind snap Generative-nonneg-CRPS VAE-lat16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=generative_nonneg --loss-function crps"

- name: precip_generative_nonneg_snap_bilinear_baseline_wd
  label: "precip snap Generative-nonneg-CRPS bilinear baseline (no tessera, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood precip=generative_nonneg --loss-function crps"

- name: precip_generative_nonneg_snap_vae_lat16_concat_with_elev_no_static_wd
  label: "precip snap Generative-nonneg-CRPS VAE-lat16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood precip=generative_nonneg --loss-function crps"


# =========================================================================
# FiLM (lat16, no-projection, with-elev) variants — backfill
#
# Direct lat16 VAE-latent injection via FiLM. Each entry is a concat->film
# swap of the corresponding existing direct-concat entry, added so every
# region has lat16+FiLM (no-proj, with-elev) coverage for:
#   - t2m   gaussian head, NLL loss
#   - wind  truncated-normal head, NLL loss
#   - generative head, CRPS loss (t2m=generative, wind=generative_nonneg)
# =========================================================================

- name: t2m_snap_vae_lat16_film_with_elev_no_static_wd
  label: "t2m snap VAE-lat16 FiLM (with elev, no static, wd=1e-4)"
  colour: "#ff7f0e"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"

- name: wind_truncnormal_snap_vae_lat16_film_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16 FiLM (with elev, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"

- name: t2m_generative_snap_vae_lat16_film_with_elev_no_static_wd
  label: "t2m snap Generative-CRPS VAE-lat16 FiLM direct (with elev, no static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood t2m=generative --loss-function crps"

- name: wind_generative_nonneg_snap_vae_lat16_film_with_elev_no_static_wd
  label: "wind snap Generative-nonneg-CRPS VAE-lat16 FiLM direct (with elev, no static, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=generative_nonneg --loss-function crps"

- name: precip_generative_nonneg_snap_vae_lat16_film_with_elev_no_static_wd
  label: "precip snap Generative-nonneg-CRPS VAE-lat16 FiLM direct (with elev, no static, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood precip=generative_nonneg --loss-function crps"


# =========================================================================
# ABLATION COMPLETION PATCH — lat16-direct concat shuffled + summary-stats
# siblings for: t2m(gaussian/NLL), wind(truncated_normal/NLL),
#               precip(generative/CRPS-ensemble).
# Each entry mirrors the live 'with elev, no static' lat16-direct concat
# config and only swaps --vae-latents-path to the shuffled / summary-stats
# latents (both global, row-aligned to ${VAE_LATENTS_CSV}).
# Appended; safe to re-run via submit.sh (3 seeds each).
# =========================================================================
- name: t2m_snap_vae_lat16_shuffled_concat_with_elev_no_static_wd
  label: "t2m snap VAE-lat16 shuffled concat (wd=1e-4)"
  colour: "#7f7f7f"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4"


- name: t2m_snap_stats_dim16_concat_with_elev_no_static_wd
  label: "t2m snap summary-stats dim16 concat (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --no-static-fields --weight-decay 1e-4"


- name: wind_truncnormal_snap_vae_lat16_shuffled_concat_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16 shuffled concat (wd=1e-4)"
  colour: "#7f7f7f"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: wind_truncnormal_snap_stats_dim16_concat_with_elev_no_static_wd
  label: "Wind (TruncNormal) snap summary-stats dim16 concat (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: precip_generative_snap_vae_lat16_shuffled_concat_with_elev_no_static_wd
  label: "precip snap Generative-CRPS VAE-lat16 shuffled concat direct (with elev, no static, wd=1e-4)"
  colour: "#7f7f7f"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_SHUFFLED} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood precip=generative --loss-function crps"


- name: precip_generative_snap_stats_dim16_concat_with_elev_no_static_wd
  label: "precip snap Generative-CRPS summary-stats dim16 concat direct (with elev, no static, wd=1e-4)"
  colour: "#17becf"
  target_variables: [precip]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_STATS_DIM16} --vae-latents-station-csv ${VAE_LATENTS_CSV} --no-static-fields --weight-decay 1e-4 --likelihood precip=generative --loss-function crps"

# =========================================================================
# Per-station mTPI ablation (Vaughan et al. 2022 elevation vector)
# =========================================================================
# Adds --use-mtpi to the with-elev bilinear baseline and the lat16
# direct-concat with-elev config, so the per-station feature vector becomes
# (elevation, delta_elevation, mTPI) instead of (elevation, delta_elevation).
# Pairs 1:1 with *_bilinear_baseline_wd and
# *_vae_lat16_concat_with_elev_no_static_wd above for the elev vs elev+mTPI
# comparison. Requires the dataset's stations.csv to carry an `mtpi` column
# (scripts/preprocessing/backfill_station_mtpi.py). Single-task only.
# +4 configs x 3 seeds = +12 jobs.


- name: wind_truncnormal_snap_bilinear_baseline_mtpi_wd
  label: "Wind (TruncNormal) snap bilinear baseline (with elev + mTPI, with static, wd=1e-4)"
  colour: "#555555"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: t2m_snap_bilinear_baseline_mtpi_wd
  label: "t2m snap bilinear baseline (with elev + mTPI, with static, wd=1e-4)"
  colour: "#555555"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --use-mtpi --weight-decay 1e-4"


- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16 concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"


- name: t2m_snap_vae_lat16_concat_with_elev_mtpi_no_static_wd
  label: "t2m snap VAE-lat16 concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#9467bd"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4"

# =========================================================================
# CAPACITY SWEEP — larger CNN for the per-region model (added 2026-06-29).
# Tests whether more model capacity pays off now that we train on far
# more data than Vaughan et al. 2022 (~128ch/6-block ResNet, Europe,
# daily, ~3k stations) — see capacity analysis. lat16 DIRECT concat +
# no-TESSERA baseline only, t2m (Gaussian) + wind (TruncNormal).
# Size flags in extra_args override the submit COMMON_ARGS (cnn128x7k3).
# =========================================================================

- name: t2m_snap_bilinear_baseline_cnn128x13k5_wd
  label: "t2m bilinear baseline (cnn128x13k5)"
  colour: "#999999"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: t2m_snap_bilinear_baseline_cnn256x13k5_wd
  label: "t2m bilinear baseline (cnn256x13k5)"
  colour: "#999999"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

- name: t2m_snap_vae_lat16_concat_with_elev_no_static_cnn128x13k5_wd
  label: "t2m VAE-lat16 concat direct (cnn128x13k5)"
  colour: "#2ca02c"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: t2m_snap_vae_lat16_concat_with_elev_no_static_cnn256x13k5_wd
  label: "t2m VAE-lat16 concat direct (cnn256x13k5)"
  colour: "#2ca02c"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_bilinear_baseline_cnn128x13k5_wd
  label: "wind (TruncNormal) bilinear baseline (cnn128x13k5)"
  colour: "#999999"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=truncated_normal --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_bilinear_baseline_cnn256x13k5_wd
  label: "wind (TruncNormal) bilinear baseline (cnn256x13k5)"
  colour: "#999999"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=truncated_normal --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_no_static_cnn128x13k5_wd
  label: "wind (TruncNormal) VAE-lat16 concat direct (cnn128x13k5)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_no_static_cnn256x13k5_wd
  label: "wind (TruncNormal) VAE-lat16 concat direct (cnn256x13k5)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

# =========================================================================
# CAPACITY SWEEP × mTPI — larger CNN + mTPI feature for the per-region model
# (added 2026-06-29). Same 2 sizes as the no-mTPI capacity block, with
# --use-mtpi added. lat16 DIRECT concat + no-TESSERA baseline, t2m + wind.
# =========================================================================

- name: t2m_snap_bilinear_baseline_mtpi_cnn128x13k5_wd
  label: "t2m bilinear baseline +mTPI (cnn128x13k5)"
  colour: "#777777"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --use-mtpi --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: t2m_snap_bilinear_baseline_mtpi_cnn256x13k5_wd
  label: "t2m bilinear baseline +mTPI (cnn256x13k5)"
  colour: "#777777"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --use-mtpi --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

- name: t2m_snap_vae_lat16_concat_with_elev_mtpi_no_static_cnn128x13k5_wd
  label: "t2m VAE-lat16 concat direct +mTPI (cnn128x13k5)"
  colour: "#1a7a1a"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --use-mtpi --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: t2m_snap_vae_lat16_concat_with_elev_mtpi_no_static_cnn256x13k5_wd
  label: "t2m VAE-lat16 concat direct +mTPI (cnn256x13k5)"
  colour: "#1a7a1a"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --use-mtpi --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_bilinear_baseline_mtpi_cnn128x13k5_wd
  label: "wind (TruncNormal) bilinear baseline +mTPI (cnn128x13k5)"
  colour: "#777777"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=truncated_normal --use-mtpi --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_bilinear_baseline_mtpi_cnn256x13k5_wd
  label: "wind (TruncNormal) bilinear baseline +mTPI (cnn256x13k5)"
  colour: "#777777"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --weight-decay 1e-4 --likelihood wind=truncated_normal --use-mtpi --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_mtpi_no_static_cnn128x13k5_wd
  label: "wind (TruncNormal) VAE-lat16 concat direct +mTPI (cnn128x13k5)"
  colour: "#c2528f"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal --use-mtpi --cnn-hidden 128 --cnn-layers 13 --cnn-kernel 5"

- name: wind_truncnormal_snap_vae_lat16_concat_with_elev_mtpi_no_static_cnn256x13k5_wd
  label: "wind (TruncNormal) VAE-lat16 concat direct +mTPI (cnn256x13k5)"
  colour: "#c2528f"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --weight-decay 1e-4 --likelihood wind=truncated_normal --use-mtpi --cnn-hidden 256 --cnn-layers 13 --cnn-kernel 5"


# =========================================================================
# JEPA-encoder latents (lat128 / lat256, noaux) — mTPI variants
# =========================================================================
# Pre-trained JEPA latents injected three ways: direct concat, a LINEAR
# projection head (proj16) -> concat, and FiLM on the raw latent. All carry
# elevation + mTPI, no static fields, latent-drop 0.0, wd=1e-4, bilinear.
# Heads match the main cross_folder_analysis comparison: Gaussian for t2m,
# truncated-normal for wind. Paths resolve from VAE_LATENTS_PATH_JEPA128 /
# _JEPA256 (see submit.sh); per-dim z-score stats auto-cache on first use.

# ---- t2m (gaussian) — jepa128 ----
- name: t2m_snap_jepa_lat128_concat_with_elev_mtpi_no_static_wd
  label: "t2m snap JEPA-lat128 concat direct (with elev + mTPI, no static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA128} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4"

- name: t2m_snap_jepa_lat128_proj16_concat_with_elev_mtpi_no_static_wd
  label: "t2m snap JEPA-lat128→proj16 concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#a0705f"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA128} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --use-mtpi --weight-decay 1e-4"

- name: t2m_snap_jepa_lat128_film_with_elev_mtpi_no_static_wd
  label: "t2m snap JEPA-lat128 FiLM raw (with elev + mTPI, no static, wd=1e-4)"
  colour: "#c49a94"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_JEPA128} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4"

# ---- t2m (gaussian) — jepa256 ----
- name: t2m_snap_jepa_lat256_concat_with_elev_mtpi_no_static_wd
  label: "t2m snap JEPA-lat256 concat direct (with elev + mTPI, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA256} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4"

- name: t2m_snap_jepa_lat256_proj16_concat_with_elev_mtpi_no_static_wd
  label: "t2m snap JEPA-lat256→proj16 concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#ec9fd4"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA256} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --use-mtpi --weight-decay 1e-4"

- name: t2m_snap_jepa_lat256_film_with_elev_mtpi_no_static_wd
  label: "t2m snap JEPA-lat256 FiLM raw (with elev + mTPI, no static, wd=1e-4)"
  colour: "#f4c7e6"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_JEPA256} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4"

# ---- wind (truncated-normal) — jepa128 ----
- name: wind_truncnormal_snap_jepa_lat128_concat_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap JEPA-lat128 concat direct (with elev + mTPI, no static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA128} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

- name: wind_truncnormal_snap_jepa_lat128_proj16_concat_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap JEPA-lat128→proj16 concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#a0705f"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA128} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

- name: wind_truncnormal_snap_jepa_lat128_film_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap JEPA-lat128 FiLM raw (with elev + mTPI, no static, wd=1e-4)"
  colour: "#c49a94"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_JEPA128} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

# ---- wind (truncated-normal) — jepa256 ----
- name: wind_truncnormal_snap_jepa_lat256_concat_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap JEPA-lat256 concat direct (with elev + mTPI, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA256} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

- name: wind_truncnormal_snap_jepa_lat256_proj16_concat_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap JEPA-lat256→proj16 concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#ec9fd4"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH_JEPA256} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --vae-latents-proj-dim 16 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

- name: wind_truncnormal_snap_jepa_lat256_film_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap JEPA-lat256 FiLM raw (with elev + mTPI, no static, wd=1e-4)"
  colour: "#f4c7e6"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection film --vae-latents-path ${VAE_LATENTS_PATH_JEPA256} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

# =========================================================================
# EXTRA DESCRIPTORS — hand-crafted surface/topography descriptors after
# Bakketun et al. 2026 (arXiv:2607.02824), added 2026-07-22. 17 dims:
# WorldCover land-cover fractions, canopy height, SoilGrids clay/sand over
# the 640 m TESSERA patch footprint + GLO-30 elevation stats / slope /
# directional derivatives over the paper's 12.5 km kernel. Two questions,
# each asked against the shortlisted main variants:
#   1. baseline + extradesc — do hand-crafted descriptors recover (part of)
#      the TESSERA gain? (learned vs hand-crafted representation)
#   2. TESSERA lat16 + extradesc — are they complementary? The combined
#      arm passes BOTH flags; train.py concatenates the two vectors
#      internally (station set identical to the pure lat16 arm, since the
#      descriptors have 0 NaN rows).
# Baseline arm keeps static fields (mirrors its parent baseline); TESSERA
# arm drops them (mirrors its parent). Both keep elev + mTPI + wd=1e-4.
# =========================================================================

- name: t2m_snap_bilinear_baseline_mtpi_extradesc_wd
  label: "t2m snap bilinear baseline + extra descriptors (with elev + mTPI, with static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --extra-descriptors-path ${EXTRA_DESCRIPTORS_PATH} --extra-descriptors-station-csv ${VAE_LATENTS_CSV} --extra-descriptors-drop-prob 0.0 --use-mtpi --weight-decay 1e-4"

- name: wind_truncnormal_snap_bilinear_baseline_mtpi_extradesc_wd
  label: "Wind (TruncNormal) snap bilinear baseline + extra descriptors (with elev + mTPI, with static, wd=1e-4)"
  colour: "#8c564b"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --extra-descriptors-path ${EXTRA_DESCRIPTORS_PATH} --extra-descriptors-station-csv ${VAE_LATENTS_CSV} --extra-descriptors-drop-prob 0.0 --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"

- name: t2m_snap_vae_lat16_plus_extradesc_concat_with_elev_mtpi_no_static_wd
  label: "t2m snap VAE-lat16 + extra descriptors concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [t2m]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --extra-descriptors-path ${EXTRA_DESCRIPTORS_PATH} --extra-descriptors-station-csv ${VAE_LATENTS_CSV} --no-static-fields --use-mtpi --weight-decay 1e-4"

- name: wind_truncnormal_snap_vae_lat16_plus_extradesc_concat_with_elev_mtpi_no_static_wd
  label: "Wind (TruncNormal) snap VAE-lat16 + extra descriptors concat (with elev + mTPI, no static, wd=1e-4)"
  colour: "#e377c2"
  target_variables: [wind]
  extra_args: "--interpolation bilinear --tessera-injection concat --vae-latents-path ${VAE_LATENTS_PATH} --vae-latents-station-csv ${VAE_LATENTS_CSV} --vae-latents-drop-prob 0.0 --extra-descriptors-path ${EXTRA_DESCRIPTORS_PATH} --extra-descriptors-station-csv ${VAE_LATENTS_CSV} --no-static-fields --use-mtpi --weight-decay 1e-4 --likelihood wind=truncated_normal"
