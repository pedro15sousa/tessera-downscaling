"""Pure helpers for building the downscaling datasets from the raw staging tree.

The scripts in ``scripts/preprocessing/`` are the entry points; this package
holds only the functions they share (see :mod:`.helpers`).
"""
